Viewed useEditorSessionRecovery.ts:154-154
Ran command: `echo -e "\n=== 5. ??????? ?????? ==="`
Ran command: `docker exec $(docker ps -q --filter name=gitea) env | grep -E "^GITEA|^USER_UID|^USER_GID|^DB"`
Ran command: `df -h /`
Ran command: `docker logs $(docker ps -q --filter name=nginx) --tail=20 2>&1`
Ran command: `DIAG`

## التشخيص واضح جداً! 🎯 وجدنا المشكلة

من تحليل النتائج:

```
"GET /ahmed-bakr1988/SaaS-PDF HTTP/1.1" 500 2104
```

**الخطأ 500 خاص بالمستودع `ahmed-bakr1988/SaaS-PDF` فقط، وليس بـ Gitea كله!**

والأهم: Gitea مُعدّ على **PostgreSQL** بمستخدم `gitea` وقاعدة `gitea` — وهذا منفصل عن قاعدة بيانات التطبيق. الغالب أن ملفات Git للمستودع تالفة أو مفقودة.

شغّل هذا على السيرفر الآن:

```bash
# 1. شاهد السجلات التفصيلية للخطأ بالضبط
docker exec $(docker ps -q --filter name=gitea) cat /data/gitea/log/gitea.log 2>/dev/null | tail -50

# 2. هل ملفات المستودع موجودة؟
docker exec $(docker ps -q --filter name=gitea) ls -la /data/git/repositories/ahmed-bakr1988/

# 3. هل مجلد SaaS-PDF.git موجود؟
docker exec $(docker ps -q --filter name=gitea) ls -la /data/git/repositories/ahmed-bakr1988/SaaS-PDF.git/ 2>/dev/null || echo "no file hear!"

# 4. فحص صحة Git للمستودع
docker exec $(docker ps -q --filter name=gitea) git -C /data/git/repositories/ahmed-bakr1988/SaaS-PDF.git fsck 2>&1 | head -30

# 5. حجم المستودع
docker exec $(docker ps -q --filter name=gitea) du -sh /data/git/repositories/ahmed-bakr1988/SaaS-PDF.git/ 2>/dev/null
```

أرسل لي الناتج وسأعطيك الحل الفوري.